import { ResultMatchCalculator } from './result-match-calculator';

describe('ResultMatchCalculator', () => {
  it('should create an instance', () => {
    expect(new ResultMatchCalculator()).toBeTruthy();
  });
});
