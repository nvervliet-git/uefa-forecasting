import { HighestGoalsMatchCalc } from './highest-goals-match-calc';

describe('HighestGoalsMatchCalc', () => {
  it('should create an instance', () => {
    expect(new HighestGoalsMatchCalc()).toBeTruthy();
  });
});
